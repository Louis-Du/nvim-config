-- Configuración para local-highlight.nvim
require("local-highlight").setup({
  file_types = nil, -- habilita en todos los tipos de archivo
  hlgroup = "Search",
  cw_hlgroup = nil,
  insert_mode = false,
})
